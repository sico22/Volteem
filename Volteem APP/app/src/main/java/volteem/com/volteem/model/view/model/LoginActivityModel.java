package volteem.com.volteem.model.view.model;

import android.arch.lifecycle.ViewModel;

public class LoginActivityModel extends ViewModel {

    ///Model is empty; no data is needed to be stored inside LoginActivity
    ///This means this Model is useless, but it is left because it may be needed later.
    public LoginActivityModel() {

    }
}